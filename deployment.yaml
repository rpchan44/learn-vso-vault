apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: webapp
  name: webapp
  namespace: development
spec:
  selector:
    matchLabels:
      app: webapp
  template:
    metadata:
      labels:
        app: webapp
    spec:
      containers:
      - image: nginx
        name: nginx
        env:
        - name: DB_USER
          valueFrom:
            secretKeyRef:
              name: dbcred
              key: username
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: dbcred
              key: password
